<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="340dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/bg_ignore_dialog">

    <!-- Header with Icon, Title, and Close cross -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <FrameLayout
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/bg_btn_ignore_danger"
            android:layout_marginEnd="12dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_trash_delete"
                app:tint="@color/dialog_danger_text"
                android:contentDescription="Удалить" />
        </FrameLayout>

        <TextView
            android:id="@+id/dialogTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Удаление комментария"
            android:textColor="@color/dialog_text_title"
            android:textSize="18sp"
            android:fontFamily="@font/open_sans_bold"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btnClose"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            app:tint="@color/dialog_icon_close"
            android:contentDescription="Закрыть"
            android:padding="4dp" />
    </LinearLayout>

    <!-- Message -->
    <TextView
        android:id="@+id/dialogMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:text="Вы уверены, что хотите удалить этот комментарий? Это действие нельзя отменить."
        android:textColor="@color/dialog_text_body"
        android:textSize="14sp"
        android:fontFamily="@font/open_sans"
        android:lineSpacingExtra="2dp" />

    <!-- Optional Comment Preview Container -->
    <LinearLayout
        android:id="@+id/commentPreviewContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="12dp"
        android:padding="12dp"
        android:background="@drawable/bg_ignore_info_card"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvCommentAuthor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/dialog_danger_text"
            android:textSize="13sp"
            android:fontFamily="@font/open_sans_bold"
            android:textStyle="bold"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tvCommentSnippet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/dialog_text_body"
            android:textSize="13sp"
            android:fontFamily="@font/open_sans"
            android:maxLines="3"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Action Buttons Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:orientation="horizontal"
        android:gravity="end|center_vertical">

        <TextView
            android:id="@+id/btnCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp"
            android:text="Отмена"
            android:textColor="@color/dialog_cancel_text"
            android:textSize="14sp"
            android:fontFamily="@font/open_sans_semibold"
            android:clickable="true"
            android:focusable="true"
            android:background="@drawable/bg_btn_cancel_ripple"
            android:layout_marginEnd="8dp" />

        <TextView
            android:id="@+id/btnDelete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="18dp"
            android:paddingVertical="10dp"
            android:text="Удалить"
            android:textColor="@color/dialog_danger_text"
            android:textSize="14sp"
            android:fontFamily="@font/open_sans_bold"
            android:textStyle="bold"
            android:background="@drawable/bg_btn_ignore_danger"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>

</LinearLayout>
